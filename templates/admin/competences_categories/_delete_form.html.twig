<form method="post" action="{{ path('admin_competences_categories_delete', {'id': competences_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competences_category.id) }}">
    <button class="btn btn-danger" style="border-radius: 5px">Delete</button>
</form>
